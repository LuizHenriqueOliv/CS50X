#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    // Pegar a chave
    string key = argv[1];
    // validar a chave
        // verificar se tem 26 caracteres
        if (strlen(key) != 26)
        {
            printf("The key dont have 26 caracteres"\n);
        }
        // verificar se são todos alphabeticos

        // verificar se são todos diferentes
    // pegar o texto

    // encriptar

    // printar o texto cifrado
}
